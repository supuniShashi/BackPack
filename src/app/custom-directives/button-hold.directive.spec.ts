import { ButtonHoldDirective } from './button-hold.directive';

describe('ButtonHoldDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonHoldDirective();
    expect(directive).toBeTruthy();
  });
});
